import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js';
const $=id=>document.getElementById(id);const menu=$('menu'),hud=$('hud'),name=$('name');let ws,meId,animals=[],players=new Map(),mobs=new Map(),scene,camera,renderer,clock;let meshes=new Map(),mobMeshes=new Map(),keys={},audio;
$('play').onclick=()=>start();name.onkeydown=e=>{if(e.key==='Enter')start()};
function start(){if(ws)return;menu.classList.add('hidden');hud.classList.remove('hidden');connect();init();startMusic()}
function connect(){ws=new WebSocket((location.protocol==='https:'?'wss://':'ws://')+location.host);ws.onmessage=e=>{let m=JSON.parse(e.data);if(m.type==='init'){meId=m.id;animals=m.animals;m.players.forEach(p=>players.set(p.id,p));m.mobs.forEach(x=>mobs.set(x.id,x))}if(m.type==='players'){players.clear();m.players.forEach(p=>players.set(p.id,p))}if(m.type==='state'){players.clear();m.players.forEach(p=>players.set(p.id,p));mobs.clear();m.mobs.forEach(x=>mobs.set(x.id,x));me=players.get(meId)}if(m.type==='damage')damagePopup(m.amount,m.x,m.z);if(m.type==='damageMob')damagePopup(m.amount,m.x,m.z)};ws.onopen=()=>ws.send(JSON.stringify({type:'join',name:name.value.trim()||'Joueur'}))}
let me;window.addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(e.key.toLowerCase()==='r'&&me?.dead)ws?.send(JSON.stringify({type:'respawn'}));if(e.code==='Space')e.preventDefault()});window.addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);window.addEventListener('mousedown',e=>{if(e.button===0&&me&&!me.dead){ws?.send(JSON.stringify({type:'attack'}));attackSound()}});$('respawn').onclick=()=>ws?.send(JSON.stringify({type:'respawn'}));
function init(){scene=new THREE.Scene();scene.background=new THREE.Color(0x9bc86b);scene.fog=new THREE.Fog(0x9bc86b,85,245);camera=new THREE.PerspectiveCamera(58,innerWidth/innerHeight,.1,400);renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;document.body.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xdfffd0,0x67513a,2.1));let sun=new THREE.DirectionalLight(0xfff5d8,2.7);sun.position.set(-40,90,30);sun.castShadow=true;sun.shadow.mapSize.set(1024,1024);scene.add(sun);makeWorld();clock=new THREE.Clock();requestAnimationFrame(loop)}
function makeWorld(){let g=new THREE.PlaneGeometry(260,260,60,60),pos=g.attributes.position;for(let i=0;i<pos.count;i++){let x=pos.getX(i),z=pos.getY(i);pos.setZ(i,Math.sin(x*.055)*1.8+Math.cos(z*.045)*1.5+Math.sin((x+z)*.02)*2)}g.computeVertexNormals();let ground=new THREE.Mesh(g,new THREE.MeshStandardMaterial({color:0x6f9b45,roughness:1}));ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;scene.add(ground);makeRiver(-48);makeRiver(52);makeWaterfall(52,-35);for(let i=0;i<210;i++){let x=(Math.random()-.5)*250,z=(Math.random()-.5)*250;if(Math.abs(x)<15&&Math.abs(z)<15)continue;Math.random()<.72?tree(x,z):rock(x,z)}let ring=new THREE.Mesh(new THREE.CylinderGeometry(12,12,.35,24),new THREE.MeshStandardMaterial({color:0x46583b}));ring.position.y=.4;scene.add(ring)}
function makeRiver(x){let water=new THREE.Mesh(new THREE.PlaneGeometry(10,260,1,1),new THREE.MeshStandardMaterial({color:0x3c94ad,transparent:true,opacity:.78,roughness:.25,metalness:.1}));water.rotation.x=-Math.PI/2;water.position.set(x,.35,0);scene.add(water);for(let i=0;i<45;i++){let foam=new THREE.Mesh(new THREE.SphereGeometry(.12+Math.random()*.22,6,4),new THREE.MeshBasicMaterial({color:0xd9f6ed,transparent:true,opacity:.35}));foam.position.set(x+rand(-4,4),.43,rand(-125,125));scene.add(foam)}}
function makeWaterfall(x,z){let cliff=new THREE.Mesh(new THREE.BoxGeometry(14,5,7),new THREE.MeshStandardMaterial({color:0x665b48,roughness:1}));cliff.position.set(x,2.2,z);scene.add(cliff);let fall=new THREE.Mesh(new THREE.PlaneGeometry(9,6),new THREE.MeshStandardMaterial({color:0x70c9df,transparent:true,opacity:.8,side:THREE.DoubleSide}));fall.position.set(x,3.3,z+3.5);scene.add(fall)}
function rand(a,b){return a+Math.random()*(b-a)}function tree(x,z){let s=.7+Math.random()*.9;let trunk=new THREE.Mesh(new THREE.CylinderGeometry(.3*s,.55*s,3.2*s,7),new THREE.MeshStandardMaterial({color:0x65452c,roughness:1}));trunk.position.set(x,1.6*s,z);trunk.castShadow=true;scene.add(trunk);for(let j=0;j<3;j++){let c=new THREE.Mesh(new THREE.IcosahedronGeometry((1.6-j*.18)*s,1),new THREE.MeshStandardMaterial({color:j===0?0x2f7436:0x3f873d,roughness:1}));c.position.set(x+rand(-.5,.5),3.2*s+j*1.2*s,z+rand(-.5,.5));c.castShadow=true;scene.add(c)}}function rock(x,z){let r=new THREE.Mesh(new THREE.DodecahedronGeometry(1+Math.random()*1.5,1),new THREE.MeshStandardMaterial({color:0x77766a,roughness:1}));r.position.set(x,.7,z);r.scale.y=.65;r.castShadow=true;scene.add(r)}
const colors=[0x85582e,0x303a2c,0x4f3c26,0x8b6a42,0xd4bb83,0x777777,0xb66c36,0x5a3b20,0x9a6d3e,0x5e5a4b,0x654b39,0xc78943,0xd99b4e,0xb46c42,0x5c4938,0x444444,0x6c5a3d,0x64554a,0x77746a,0x9d895e];
function animalMesh(lvl,player=false){let s=animals[lvl-1]?.[4]||1,c=colors[lvl-1]||0x777;let g=new THREE.Group();let body=new THREE.Mesh(new THREE.SphereGeometry(1,10,7),new THREE.MeshStandardMaterial({color:c,roughness:.9}));body.scale.set(1.25*s,.82*s,1.65*s);body.position.y=1.0*s;g.add(body);let head=new THREE.Mesh(new THREE.SphereGeometry(.7,10,7),new THREE.MeshStandardMaterial({color:c,roughness:.85}));head.position.set(0,1.5*s,1.45*s);g.add(head);for(let side of [-1,1]){let eye=new THREE.Mesh(new THREE.SphereGeometry(.09*s,6,5),new THREE.MeshBasicMaterial({color:0x111111}));eye.position.set(side*.25*s,1.65*s,2.05*s);g.add(eye)}for(let x of [-.55,.55])for(let z of [-.6,.65]){let leg=new THREE.Mesh(new THREE.CylinderGeometry(.13*s,.2*s,.9*s,6),new THREE.MeshStandardMaterial({color:c}));leg.position.set(x*s,.48*s,z*s);g.add(leg)}
if(lvl<=6){for(let side of [-1,1]){let ear=new THREE.Mesh(new THREE.ConeGeometry(.22*s,.65*s,5),new THREE.MeshStandardMaterial({color:c}));ear.position.set(side*.45*s,1.95*s,1.35*s);g.add(ear)}}if([8,9,10,11,13,14,15,16,17,18,19,20].includes(lvl)){let tail=new THREE.Mesh(new THREE.CylinderGeometry(.1*s,.18*s,1.8*s,6),new THREE.MeshStandardMaterial({color:c}));tail.rotation.x=Math.PI/2;tail.position.set(0,1*s,-1.9*s);g.add(tail)}if([9,13,14,17,19].includes(lvl))for(let side of [-1,1]){let horn=new THREE.Mesh(new THREE.ConeGeometry(.12*s,.8*s,6),new THREE.MeshStandardMaterial({color:0xd8c99b}));horn.position.set(side*.35*s,2*s,1.25*s);horn.rotation.z=side*.45;g.add(horn)}if(lvl===20){let trunk=new THREE.Mesh(new THREE.CylinderGeometry(.16*s,.25*s,1.8*s,7),new THREE.MeshStandardMaterial({color:c}));trunk.rotation.x=Math.PI/2;trunk.position.set(0,1.15*s,2.2*s);g.add(trunk)}if(player){let aura=new THREE.Mesh(new THREE.RingGeometry(1.1*s,1.22*s,24),new THREE.MeshBasicMaterial({color:0xb9ee55,transparent:true,opacity:.55,side:THREE.DoubleSide}));aura.rotation.x=-Math.PI/2;aura.position.y=.05;g.add(aura)}return g}
function healthBar(obj,hp,max){if(!obj)return;let old=obj.getObjectByName('hp');if(old)obj.remove(old);let group=new THREE.Group();group.name='hp';let bg=new THREE.Mesh(new THREE.PlaneGeometry(2.1,0.16),new THREE.MeshBasicMaterial({color:0x151515}));let fill=new THREE.Mesh(new THREE.PlaneGeometry(2.0,0.1),new THREE.MeshBasicMaterial({color:0xe65c62}));fill.position.z=.01;fill.scale.x=Math.max(0,hp/max);fill.position.x=-(1-fill.scale.x);group.add(bg,fill);group.position.y=3.1;group.rotation.x=-.08;obj.add(group)}
function sync(){for(let [id,p] of players){let m=meshes.get(id);if(!m){m=animalMesh(p.level,true);scene.add(m);meshes.set(id,m)}if(m.userData.level!==p.level){let q=m.position.clone(),r=m.rotation.y;scene.remove(m);m=animalMesh(p.level,true);m.position.copy(q);m.rotation.y=r;scene.add(m);meshes.set(id,m);m.userData.level=p.level}m.position.set(p.x,0,p.z);m.rotation.y=p.rot;m.visible=!p.dead&&!p.spawnProtected;healthBar(m,p.hp,p.maxHp)}for(let [id,x] of mobs){let m=mobMeshes.get(id);if(!m){m=animalMesh(x.level);scene.add(m);mobMeshes.set(id,m)}m.position.set(x.x,0,x.z);m.rotation.y=x.rot;m.visible=true;healthBar(m,x.hp,x.maxHp)}}
let last=0;function loop(t){requestAnimationFrame(loop);if(t-last>45){sendMove();last=t}sync();if(me){let m=meshes.get(meId);if(m){let back=10+animals[me.level-1][4]*1.7;let target=new THREE.Vector3(m.position.x-Math.sin(me.rot)*back,5.8+animals[me.level-1][4]*1.2,m.position.z-Math.cos(me.rot)*back);camera.position.lerp(target,.12);camera.lookAt(m.position.x,1.1,m.position.z)}hudUpdate()}renderer.render(scene,camera)}
function sendMove(){if(!ws||ws.readyState!==1||!me||me.dead)return;let f=(keys.z?1:0)+(keys.s?-1:0),t=(keys.d?1:0)+(keys.q?-1:0);if(f||t)ws.send(JSON.stringify({type:'move',forward:f,turn:t}))}
function hudUpdate(){let a=animals[me.level-1];$('animal').textContent=a[0];$('level').textContent=me.level;$('lvlBig').textContent=me.level;$('power').textContent=a[3];$('xpText').textContent=`${Math.floor(me.xp)} / ${a[2]}`;$('hpText').textContent=`${Math.max(0,Math.ceil(me.hp))} / ${me.maxHp}`;$('xpbar').style.width=Math.min(100,me.xp/a[2]*100)+'%';$('hpbar').style.width=Math.max(0,me.hp/me.maxHp*100)+'%';let z=me.level<=2?1:me.level<=5?2:me.level<=8?3:me.level<=11?4:me.level<=14?5:me.level<=17?6:7;$('zone').textContent='ZONE '+z+(z===7?' — COLOSSES':'');$('dead').classList.toggle('hidden',!me.dead);$('scores').innerHTML=[...players.values()].sort((a,b)=>b.level-a.level||b.xp-a.xp).slice(0,10).map((p,i)=>`<div class="row ${p.id===meId?'me':''}"><span>${i+1}. ${esc(p.name)}</span><span>Niv. ${p.level}</span></div>`).join('')}
function esc(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function damagePopup(amount,x,z){let v=document.createElement('div');v.className='damage';v.textContent='-'+amount;v.style.left='50%';v.style.top='42%';document.body.appendChild(v);setTimeout(()=>v.remove(),700)}
function attackSound(){try{if(!audio)return;let o=audio.createOscillator(),g=audio.createGain();o.type='sawtooth';o.frequency.setValueAtTime(150,audio.currentTime);o.frequency.exponentialRampToValueAtTime(60,audio.currentTime+.1);g.gain.setValueAtTime(.025,audio.currentTime);g.gain.exponentialRampToValueAtTime(.001,audio.currentTime+.12);o.connect(g).connect(audio.destination);o.start();o.stop(audio.currentTime+.13)}catch{}}
function startMusic(){try{audio=new (window.AudioContext||window.webkitAudioContext)();let master=audio.createGain();master.gain.value=.018;master.connect(audio.destination);let notes=[196,246.94,293.66,246.94,220,261.63,329.63,261.63],i=0;setInterval(()=>{if(audio.state==='suspended')audio.resume();let o=audio.createOscillator(),g=audio.createGain();o.type='sine';o.frequency.value=notes[i++%notes.length];g.gain.setValueAtTime(0,audio.currentTime);g.gain.linearRampToValueAtTime(1,audio.currentTime+.15);g.gain.exponentialRampToValueAtTime(.001,audio.currentTime+1.4);o.connect(g).connect(master);o.start();o.stop(audio.currentTime+1.45)},900)}catch{}}
window.onresize=()=>{if(camera&&renderer){camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)}};
